apiVersion: v1
kind: Service
metadata:
  name: icicso-redis
  namespace: icicso-local
  labels:
    app.kubernetes.io/name: icicso-redis
    app.kubernetes.io/part-of: icicso
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: icicso-redis
  ports:
    - name: redis
      port: 6379
      targetPort: redis

{
  "annotations": { "list": [] },
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "gridPos": { "h": 8, "w": 8, "x": 0, "y": 0 },
      "id": 1,
      "options": { "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false } },
      "targets": [
        { "expr": "sum(rate(icicso_documents_received_total[5m]))", "refId": "A" }
      ],
      "title": "Documents Received",
      "type": "stat"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "gridPos": { "h": 8, "w": 8, "x": 8, "y": 0 },
      "id": 2,
      "options": { "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false } },
      "targets": [
        { "expr": "sum(rate(icicso_parse_success_total[5m]))", "refId": "A" }
      ],
      "title": "Parse Success Rate",
      "type": "stat"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "gridPos": { "h": 8, "w": 8, "x": 16, "y": 0 },
      "id": 3,
      "options": { "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false } },
      "targets": [
        { "expr": "sum(rate(icicso_parse_failure_total[5m]))", "refId": "A" }
      ],
      "title": "Parse Failure Rate",
      "type": "stat"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "gridPos": { "h": 8, "w": 24, "x": 0, "y": 8 },
      "id": 4,
      "targets": [
        { "expr": "histogram_quantile(0.95, sum by (le, document_type) (rate(icicso_parse_duration_seconds_bucket[5m])))", "legendFormat": "{{document_type}} p95", "refId": "A" }
      ],
      "title": "Parser Duration P95 by Document Type",
      "type": "timeseries"
    }
  ],
  "schemaVersion": 39,
  "tags": ["icicso", "parser"],
  "time": { "from": "now-6h", "to": "now" },
  "title": "ICICSO Parser Health",
  "uid": "icicso-parser-health",
  "version": 1
}

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: icicso-observability-data
  namespace: icicso-local
  labels:
    app.kubernetes.io/name: icicso-observability
    app.kubernetes.io/component: observability
    app.kubernetes.io/part-of: icicso
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
